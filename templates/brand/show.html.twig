{% extends 'base.html.twig' %}

{% block title %}Brand
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="title text-center mt-3">Brand</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ brand.id }}</td>
				</tr>
				<tr>
					<th>Name</th>
					<td>{{ brand.name }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('app_brand_index') }}">back to list</a>

		<a href="{{ path('app_brand_edit', {'id': brand.id}) }}">edit</a>

		{{ include('brand/_delete_form.html.twig') }}
	</div>
{% endblock %}
